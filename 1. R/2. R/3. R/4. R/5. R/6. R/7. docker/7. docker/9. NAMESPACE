# Generated by roxygen2: do not edit by hand

# Core CIAM functions
export(ciam_model)
export(causal_intervention)
export(hierarchical_attention)
export(apply_attention)

# Meta-learning functions
export(meta_select)
export(pretrained_models)

# Information Bottleneck Theorem functions
export(estimate_mutual_information)
export(check_theorem1)
export(compression_gain)

# Evaluation functions
export(evaluate_ood)
export(load_sample_data)
export(normalize_data)
export(compute_shap_values)

# Visualization functions
export(plot_feature_space)
export(plot_attention_maps)
export(plot_intervention_analysis)
export(plot_shap_summary)

# S3 methods
S3method(print, ciam_model)
S3method(plot, ciam_model)
S3method(predict, ciam_model)
S3method(fitted, ciam_model)
S3method(print, sample_data)
S3method(print, ood_evaluation)
S3method(print, shap_values)
S3method(print, theorem1_check)
S3method(print, meta_recommendations)

# Hard attention classes
S3method(apply_attention, hard_attention)
S3method(apply_attention, hybrid_attention)
S3method(apply_attention, self_attention)

# Import from other packages
importFrom(ggplot2, ggplot)
importFrom(ggplot2, aes)
importFrom(ggplot2, geom_point)
importFrom(ggplot2, geom_line)
importFrom(ggplot2, geom_tile)
importFrom(ggplot2, geom_boxplot)
importFrom(ggplot2, theme_minimal)
importFrom(ggplot2, labs)
importFrom(ggplot2, scale_fill_gradient)
importFrom(ggplot2, scale_color_gradient)
importFrom(ggplot2, scale_shape_manual)
importFrom(ggplot2, coord_flip)
importFrom(ggplot2, geom_vline)
importFrom(ggplot2, annotate)

importFrom(stats, prcomp)
importFrom(stats, runif)
importFrom(stats, rnorm)
importFrom(stats, sd)
importFrom(stats, predict)

importFrom(utils, read.csv)
importFrom(utils, write.csv)

# Conditional imports
if (requireNamespace("moments", quietly = TRUE)) {
  importFrom(moments, skewness)
  importFrom(moments, kurtosis)
}

if (requireNamespace("shapviz", quietly = TRUE)) {
  importFrom(shapviz, shapviz)
}

if (requireNamespace("Rtsne", quietly = TRUE)) {
  importFrom(Rtsne, Rtsne)
}

if (requireNamespace("umap", quietly = TRUE)) {
  importFrom(umap, umap)
}

if (requireNamespace("gridExtra", quietly = TRUE)) {
  importFrom(gridExtra, grid.arrange)
}

if (requireNamespace("tidyr", quietly = TRUE)) {
  importFrom(tidyr, pivot_longer)
}

# Use directives for package documentation
useDynLib(AdaptVisR, .registration = TRUE)

# Package documentation
#' @keywords internal
"_PACKAGE"

## usethis namespace: start
## usethis namespace: end
NULL
